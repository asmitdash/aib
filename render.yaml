services:
  - type: web
    name: aib
    runtime: node
    plan: free
    region: oregon
    branch: main
    autoDeploy: true
    buildCommand: corepack enable && pnpm config set strict-builds false --location project && pnpm install --frozen-lockfile --ignore-scripts && pnpm build
    startCommand: pnpm start
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: "22"
      - key: PNPM_VERSION
        value: "11.5.0"
      - key: AIB_BUDGET_USD
        value: "0.50"
      - key: AIB_BUDGET_OUTPUT_TOKENS
        value: "50000"
      - key: AIB_LLM_PROVIDER
        sync: false
      - key: GOOGLE_API_KEY
        sync: false
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: OPENAI_API_KEY
        sync: false
      - key: AIB_MODEL_GENERATION
        sync: false
      - key: AIB_MODEL_CLASSIFICATION
        sync: false
